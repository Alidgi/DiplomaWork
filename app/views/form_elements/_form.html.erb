<%= form_for(form_element) do |f| %>
  <% if form_element.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(form_element.errors.count, "error") %> prohibited this form_element from being saved:</h2>

      <ul>
      <% form_element.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :element_id %>
    <%= f.text_field :element_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :value %>
    <%= f.text_field :value %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
