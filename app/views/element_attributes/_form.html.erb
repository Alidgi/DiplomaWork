<%= form_for(element_attribute) do |f| %>
  <% if element_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(element_attribute.errors.count, "error") %> prohibited this element_attribute from being saved:</h2>

      <ul>
      <% element_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :attribute_id %>
    <%= f.text_field :attribute_id %>
  </div>

  <div class="field">
    <%= f.label :element_id %>
    <%= f.text_field :element_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
